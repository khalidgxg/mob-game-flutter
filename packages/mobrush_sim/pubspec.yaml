name: mobrush_sim
description: Engine-independent battle simulation for MobRush, ported from the Unity C# source.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  mobrush_data:
    path: ../mobrush_data

dev_dependencies:
  test: ^1.25.0
