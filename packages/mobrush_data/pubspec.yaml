name: mobrush_data
description: Content models for MobRush — characters, cannons, reward rules — loaded from the JSON ContentExporter emits from Unity.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
